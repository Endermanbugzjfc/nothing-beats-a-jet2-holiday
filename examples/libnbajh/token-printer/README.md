## token-printer

Example C program that uses the libnbajh lexer API to read the tokens of a Nothing Beats a Jet2 Holiday source file and print them back to standard output with respect to
their line and column numbers.
